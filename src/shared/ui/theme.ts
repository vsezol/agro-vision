export interface Theme {
  colors: {
    white: string;
    background: string;
    active: string;
    gray: string;
    darkGray: string;
  };
}

export const theme: Theme = {
  colors: {
    white: "#FFFFFF",
    background: "#F5F5F5",
    active: "#111111",
    gray: "#BCBCBC",
    darkGray: "#707070",
  },
};
