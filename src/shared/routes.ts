export enum Routes {
  Rating = "/",
  Params = "/params",
  About = "/about",
  Auth = "/auth",
  RestoreAccess = "/restore",
  RestoreAccessSuccess = "/restore-success",
  ReportBug = "/report-bug",
}
